{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build example/basic",
            "type": "shell",
            "command": "pushd ${workspaceFolder}/examples/basic && make && popd"
        },
        {
            "label": "build example/image",
            "type": "shell",
            "command": "pushd ${workspaceFolder}/examples/image && make && popd"
        }
    ]
}